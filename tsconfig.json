{
  "extends": "@dml/tsconfig/base.json",
  "docs": "Root project: type-checks the repo tooling under scripts/. Each app/package owns its own tsconfig and is checked via `pnpm -r typecheck`.",
  "compilerOptions": {
    "types": ["node"],
    "baseUrl": ".",
    "paths": {
      "#scripts/*": ["scripts/*"]
    }
  },
  "include": ["scripts/**/*.ts"]
}
